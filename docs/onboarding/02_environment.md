# 开发环境与工具

## 必装
- Node.js：建议 18+
- Git：建议安装 Git for Windows
- VS Code：建议安装团队统一扩展包（如果有）

## 建议配置
- 终端：PowerShell / Windows Terminal
- 包管理：npm（如用 pnpm/yarn 这里替换）
- 浏览器：Chrome（建议开启开发者工具常用面板）

## 验收标准 ✅
- `node -v` 能输出版本号
- `git --version` 能输出版本号
- 能 `npm install` 并运行项目
